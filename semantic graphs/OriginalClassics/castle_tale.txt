rel(castle_tale,castle,attr,strong).
rel(castle_tale,castle,have,towers).
rel(castle_tale,castle,gender,neuter).
rel(castle_tale,forest,attr,dark).
rel(castle_tale,forest,have,trees).
rel(castle_tale,forest,gender,neuter).
rel(castle_tale,king,live,castle).
rel(castle_tale,king,attr,stern).
rel(castle_tale,king,gender,male).
rel(castle_tale,king,father_of,princess).   *
rel(castle_tale,knight,attr,handsome).
rel(castle_tale,knight,attr,brave).
rel(castle_tale,knight,have,spear).
rel(castle_tale,knight,have,knife).
rel(castle_tale,knight,gender,male).
rel(castle_tale,knight,love,princess).
rel(castle_tale,knife,attr,sharp).
rel(castle_tale,knife,gender,neuter).
rel(castle_tale,lioness,live,forest).
rel(castle_tale,lioness,attr,fierce).
rel(castle_tale,lioness,attr,hungry).
rel(castle_tale,lioness,gender,female).
rel(castle_tale,princess,attr,pretty).
rel(castle_tale,princess,attr,blonde).
rel(castle_tale,princess,live,castle).
rel(castle_tale,princess,daughter_of,king).
rel(castle_tale,princess,love,knight).
rel(castle_tale,princess,gender,female).
rel(castle_tale,spear,gender,neuter).
rel(castle_tale,trees,gender,neuter).
rel(castle_tale,towers,gender,neuter).