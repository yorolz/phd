package ck.ia;

/**
 * Used to store / distinguish the type (on the entity hierarchy) of some entity
 * 
 * @author Administrator
 * 
 */
public enum EntityType {
	UNDEFINED, MOVING_AGENT, SPAWN_AGENT;
}
